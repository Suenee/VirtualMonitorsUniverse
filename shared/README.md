# Shared

Shared contracts and definitions used across VMU components.

Expected contents include protocol schemas, API contracts, monitor/state models, shared constants and generated definitions where appropriate. This directory is the canonical place for cross-component interfaces so Server, Client and Companion stay synchronized.
